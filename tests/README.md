# tests

Initial test coverage is provided through CTest smoke tests defined in the root
`CMakeLists.txt`. This directory is reserved for milestone-based expansion:

- parser tests
- semantic tests
- runtime tests
- docs example conformance tests
